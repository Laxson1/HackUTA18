<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tarleton.hackuta.workoutbuddy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".StatTrackerActivity"
            android:theme="@style/dark_redtone">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".homePage" />
        <activity android:name=".gymsNearMe" />
        <activity android:name=".workoutBuddiesNearMe" />
        <activity android:name=".settings" />
        <activity android:name=".WorkoutsNearMe"></activity>
    </application>

</manifest>